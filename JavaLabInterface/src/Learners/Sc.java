package Learners;

import java.util.ArrayList;

public interface Sc {
    ArrayList<Schoolboy> arraySc(ArrayList<Schoolboy> scList);
    Schoolboy bestSc(ArrayList<Schoolboy> sc);
    ArrayList<Schoolboy> arrayRateSc(ArrayList<Schoolboy> scList);
}

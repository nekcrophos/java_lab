package Learners;

import java.util.ArrayList;

public interface St {
    ArrayList<Student> arraySt(ArrayList<Student> stList);
    Student bestSt(ArrayList<Student> st);
    ArrayList<Student> arrayRateSt(ArrayList<Student> stList);
}
